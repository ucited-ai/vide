# Optional: Vide Connect source builds
# Leave these unset to disable optional Vide Connect features in local source builds.
# Release builds inject their public values at build time. Do not add server-side
# secrets to this file.

# Get these from the Clerk Dashboard under API keys, JWT templates, and OAuth applications.
# VIDE_CLERK_PUBLISHABLE_KEY=pk_test_...
# VIDE_CLERK_JWT_TEMPLATE=vide-relay
# VIDE_CLERK_CLI_OAUTH_CLIENT_ID=oauthapp_...

# Optional: signed macOS passkey builds. The RP domain defaults to the Frontend API
# hostname encoded in VIDE_CLERK_PUBLISHABLE_KEY. Set the override only when Clerk
# returns a different RP ID or when multiple domains must be entitled.
# VIDE_APPLE_TEAM_ID=ABC1234567
# VIDE_MACOS_PROVISIONING_PROFILE=/absolute/path/to/vide.provisionprofile
# VIDE_CLERK_PASSKEY_RP_DOMAINS=example.clerk.accounts.dev,clerk.example.com

# Get this from your relay deployment. `infra/relay` deploys update it automatically.
# VIDE_RELAY_URL=https://relay.example.com

# Optional: hosted app origin used by the CLI's out-of-band OAuth flow.
# Defaults to https://app.vide.local; override to test against a staging deployment.
# VIDE_HOSTED_APP_URL=https://nightly.app.vide.local

# Public, ingest-only mobile OpenTelemetry configuration.
# VIDE_MOBILE_OTLP_TRACES_URL=https://api.axiom.co/v1/traces
# VIDE_MOBILE_OTLP_TRACES_DATASET=vide-mobile-traces-dev
# VIDE_MOBILE_OTLP_TRACES_TOKEN=xaat-...
